This code was written and compiled using a text editor and command prompt.
Thus, compile the driver using "javac Driver.java"
To run the code, use "java Driver"
Follow the program requested input, don't screw up the input or the program will throw errors
Instructions for input.txt format are on the file.
